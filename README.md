# Vasudeva Apartments · WOW Website

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000

## Edit content
- `lib/site.ts` holds the project data, phone numbers, brochure links, gallery images and floor plans.

## Notes
- Hero video swaps automatically for mobile/desktop.
- Smooth scrolling uses Lenis (disabled when `prefers-reduced-motion` is enabled).
